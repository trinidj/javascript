function showMessage() {
  let message = "Hello, I'm JavaScript!"; //* local variable

  console.log(message);
}

showMessage();

console.log(message);

/*
  The variable message, can only be accessed within the scope of the function
*/